import { View, Text } from 'react-native';

interface TaskItemProps {
   task: {
      id: number;
      title: string;
      completed: boolean;
   }
}
export default function TodoListItem() {
 return(
      <View>
 }
}